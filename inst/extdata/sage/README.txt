Sage executable

The Windows package includes sage.exe in this directory. ProtVis locates it
automatically with protvis_sage_executable(). On Linux/macOS, install Sage
separately and ensure that `sage` is on PATH.
