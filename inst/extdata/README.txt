Maxquant_Export.xlsx is bundled as the reproducible built-in MaxQuant example.
The ProtVis_dataset importer removes MaxQuant rows marked as Reverse,
Potential contaminant, or Only identified by site by default, then stores the
30 reporter-intensity sample columns and protein metadata in a standard object.
